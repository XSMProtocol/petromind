name = "petromind-backend"
main = "worker.js"
compatibility_date = "2024-01-01"

# Your Cloudflare account ID (get it from Cloudflare dashboard)
# account_id = "YOUR_ACCOUNT_ID"

[vars]
# Environment variables (these are public)
# Don't put secrets here!

# Secrets (use: wrangler secret put ANTHROPIC_API_KEY)
# ANTHROPIC_API_KEY will be stored securely in Cloudflare
